import { Reducer } from 'redux';

// import { MapState } from '../MapState';
// import { MapAction } from '../actions/types';

// import {mapSearchReducer} from './mapSearch';
// import {mapBaseReducer} from './mapBase';

// export const mapReducer: Reducer<MapState, MapAction> = (mapState, action) => {

export const mapReducer: Reducer = () => {

  return {
    // mapSearch: mapSearchReducer(mapState && mapState.mapSearch, action),
    // mapBase: mapBaseReducer(mapState && mapState.mapBase, action)
  };
};
